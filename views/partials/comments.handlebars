<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      <p class="meta-user">{{user.username}} on {{format_date created_at}}</p>
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>